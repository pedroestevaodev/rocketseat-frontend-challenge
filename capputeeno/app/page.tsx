import { FilterBar } from "@/components/filter-bar";
import { MainBase } from "@/styles/globals";

const Home = () => {
	return (
		<MainBase>
			<FilterBar />
		</MainBase>
	);
};

export default Home;
